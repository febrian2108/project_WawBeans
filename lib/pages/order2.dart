import 'package:flutter/material.dart';

class Order2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Order 2 Page'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Order 2.'),
      ),
    );
  }
}
